#include <iostream>
using namespace std;

int main()
{
    int x;

    cin>>x;
    if (x>1)
    cout<<"x is greater than 1.";
    else if (x<1)
    cout<<"x is smaller than 1.";
    else
    cout<<"x is 0";

    return 0;
}